# ───────────────────────────────────────────────────────────
# 白名单模式:默认忽略仓库根下的一切,只放行「工具本体」。
# 目的:本工作区同时放着真实候选人数据(各岗位文件夹),而 git 仓库是公开的,
#       绝不能把任何岗位数据推上去。新建的岗位文件夹会被自动忽略,无需手动加。
# ───────────────────────────────────────────────────────────

# 默认忽略根下所有条目
/*

# 仅放行工具本体(白名单)
!/.gitignore
!/CLAUDE.md
!/AGENTS.md
!/README.md
!/LICENSE
!/.claude/
!/_共享/
!/assets/

# 即便在放行目录内,这些也绝不进仓库(纵深防御)
/_SKILL-pxb-liepin   # Obsidian 用的 SKILL junction(本地软链,别进仓库)
*.pdf
**/待定/
.search-lock
.liepin-cli/
node_modules/
.DS_Store
Thumbs.db
*.log
